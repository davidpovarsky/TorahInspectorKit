# TorahInspectorKit

Reusable, provider-independent Torah Inspector core logic and SwiftUI interface extracted from Pinkha.

The package exposes two library products:

- `TorahInspectorCore`
- `TorahInspectorUI`
